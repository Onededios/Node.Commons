{
  "name": "node-commons",
  "$schema": "node_modules/nx/schemas/project-schema.json",
  "tags": ["type:library", "scope:onededios"],
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run"
      }
    },
    "test:coverage": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --coverage"
      }
    }
  }
}
